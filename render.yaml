version: "1"
services:
  - type: web
    name: tct
    runtime: docker
    repo: https://github.com/i-tct/tct
    plan: free
    region: virginia
    dockerContext: .
    dockerfilePath: ./Dockerfile
    autoDeployTrigger: commit

    envVars:
      # Compulsory: must be set manually
      - key: SESSION_ID
        sync: false

      - key: TIMEZONE
        sync: false

      - key: PREFIX
        sync: false

      - key: DASHBOARD_USER
        sync: true

      - key: DASHBOARD_PASS
        sync: false

      - key: server_port
        value: "3010"
